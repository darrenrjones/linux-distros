doctype html
html
  head
    title= 'Linux to use Linux'
    meta(name="viewport" content="width=device-width, initial-scale=1")
  body
    h1 Install #[a(href=url) #{distro} ], never look back.
    p Rank: #{rank}